package main;

import "fmt";

func plus(a int, b int) int {
  return a + b;
}

func plusPlus(a, b, c int) (string, int) {
	if 
			fmt.Println("loop");
			fmt := alalala;
      break;
  }
  return "oi", a + b + c;
}

func main() {
	var res;
	oi = a();
  if (love == (20)) {
    lala = avante;
  }
}